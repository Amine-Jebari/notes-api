# Dev/test-only dependencies. Kept SEPARATE from requirements.txt so the
# production Docker image stays small and doesn't ship test tooling.
# Install locally with:  pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt
pytest==9.1.1
httpx==0.28.1
